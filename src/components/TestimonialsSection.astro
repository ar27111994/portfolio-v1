---
interface Props {
  testimonials: import("../data/site-types").Testimonial[];
}

const { testimonials } = Astro.props;
---

<section class="panel testimonials-panel" aria-labelledby="testimonials-title">
  <div class="section-head">
    <h2 id="testimonials-title">What clients & colleagues say</h2>
    <p>
      Endorsements from LinkedIn and Upwork across enterprise, freelance, and
      team-collaboration engagements.
    </p>
  </div>
  <div class="testimonials-grid">
    {
      testimonials.map((t) => (
        <blockquote class="testimonial-card">
          <p class="testimonial-quote">"{t.quote}"</p>
          <footer class="testimonial-footer">
            <cite>
              <strong>{t.author}</strong>
              <span class="testimonial-role">{t.role}</span>
              <span class="testimonial-context">{t.relation}</span>
            </cite>
            <a
              href={t.href}
              target="_blank"
              rel="noreferrer"
              class="testimonial-source"
            >
              {t.platform}
            </a>
          </footer>
        </blockquote>
      ))
    }
  </div>
</section>
